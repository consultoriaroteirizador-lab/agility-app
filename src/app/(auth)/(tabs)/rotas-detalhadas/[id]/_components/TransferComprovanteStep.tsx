import { Box, Text } from '@/components';

/**
 * Etapa 2 (comprovante) da tela de transferência: quem recebeu, foto/assinatura
 * e o disparo do handoff. Stub — implementação completa na Task 4.
 */
export function TransferComprovanteStep(_props: { routingId: string; onBack: () => void }) {
    return (
        <Box>
            <Text>Comprovante (em construção)</Text>
        </Box>
    );
}
