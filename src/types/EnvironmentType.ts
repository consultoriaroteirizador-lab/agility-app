// Este arquivo foi movido para src/config/environment.ts
// Mantido apenas para compatibilidade - use os exports de @/config/environment

export type { EnvironmentType } from '@/config/environment';
export type { Urls } from '@/config/urls';
export { currentEnvironment, isProduction, isDevelopment, API_KEY } from '@/config/environment';
