// Este arquivo foi movido para src/config/environment.ts
// Mantido apenas para compatibilidade - use os exports de @/config/environment

export type { EnvironmentType, Urls } from '@/config/urls';
export { currentEnvironment, isProduction, isDevelopment, isStaging, API_KEY } from '@/config/environment';
